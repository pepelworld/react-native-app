import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    error: {
        paddingTop: 8,
        paddingBottom: 8,
        color: 'red',
        textAlign: 'right',
    },
});

export default styles;
