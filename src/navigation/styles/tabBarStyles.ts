import { COLORS } from '../../constants/theme';

export const tabBarItemStyle: {} = {
    marginTop: 10,
};
export const tabBarActiveTintColor: string = COLORS.loader;
export const tabBarInactiveTintColor: string = COLORS.inactiveIcon;

export const tabBarTheme: {} = {
    tabBarItemStyle,
    tabBarActiveTintColor,
    tabBarInactiveTintColor,
};
